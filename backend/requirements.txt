fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
sqlalchemy>=2.0.36
psycopg[binary]>=3.3.0
chromadb>=0.5.23
bcrypt>=4.2.0
pyjwt>=2.10.0
httpx>=0.27.2
python-dotenv>=1.0.1
pytest>=8.3.0
# Ollama se invoca por HTTP con httpx (API /api/generate). No se añade el SDK ollama.
# Evaluación RAGAS (herramienta fuera de domain/application).
# 0.3.9 no importa con langchain-community 0.4 (falta chat_models.vertexai).
# 0.4.3 es la versión compatible con Python 3.13 y el langchain que RAGAS instala.
ragas==0.4.3
